---
import ListPage from "../../../components/ListPage.astro";
import { getLocalizedArticles } from "../../../lib/content";

const locale = "en";
const entries = await getLocalizedArticles(locale);
---

<ListPage locale={locale} collection="articles" entries={entries} alternateLocaleHref="/articles/" />
